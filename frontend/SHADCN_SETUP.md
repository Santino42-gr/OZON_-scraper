# Настройка shadcn/ui

## ✅ Выполненные шаги

Конфигурация shadcn/ui была успешно добавлена в проект:

### Созданные файлы:
- ✅ `components.json` - основной конфигурационный файл shadcn
- ✅ `tailwind.config.ts` - конфигурация Tailwind CSS
- ✅ `postcss.config.js` - конфигурация PostCSS
- ✅ `src/lib/utils.ts` - утилита для объединения классов
- ✅ `src/components/ui/` - директория для UI компонентов

### Обновленные файлы:
- ✅ `tsconfig.json` - добавлены path aliases
- ✅ `tsconfig.node.json` - добавлены типы Node.js
- ✅ `vite.config.ts` - добавлена поддержка path aliases
- ✅ `package.json` - добавлены необходимые зависимости
- ✅ `src/styles/index.css` - добавлены Tailwind директивы и CSS переменные

### Настроенные регистры:
- `@shadcn` - официальные компоненты shadcn/ui
- `@aceternity` - дополнительные компоненты Aceternity UI

## 📦 Установка зависимостей

Перейдите в директорию frontend и установите зависимости:

```bash
cd frontend
npm install
```

## 🎨 Использование компонентов shadcn

Теперь вы можете добавлять компоненты shadcn с помощью CLI или MCP сервера:

### Через CLI:
```bash
# Добавить кнопку
npx shadcn@latest add button

# Добавить карточку
npx shadcn@latest add card

# Добавить форму
npx shadcn@latest add form

# Из регистра Aceternity
npx shadcn@latest add @aceternity/hero-section
```

### Через MCP сервер в Cursor:
MCP сервер shadcn уже настроен и доступен в Cursor. Используйте его для:
- Поиска компонентов
- Просмотра примеров использования
- Автоматического добавления компонентов

## 🔧 Использование в коде

После добавления компонента, импортируйте его следующим образом:

```tsx
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

function MyComponent() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Заголовок</CardTitle>
      </CardHeader>
      <CardContent>
        <Button>Нажми меня</Button>
      </CardContent>
    </Card>
  )
}
```

## 🎨 Темная тема

Для переключения темы добавьте класс `dark` к элементу `<html>`:

```tsx
// Светлая тема
<html>

// Темная тема
<html className="dark">
```

## 📚 Дополнительные ресурсы

- [Документация shadcn/ui](https://ui.shadcn.com)
- [Компоненты shadcn/ui](https://ui.shadcn.com/docs/components)
- [Aceternity UI](https://ui.aceternity.com)
- [Tailwind CSS](https://tailwindcss.com)

## 🚀 Запуск проекта

```bash
cd frontend
npm run dev
```

Проект будет доступен по адресу: http://localhost:5173

