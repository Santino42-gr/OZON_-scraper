# Shared - Общие утилиты и типы

Общие модули, используемые в backend, bot и frontend.

## 📁 Структура

```
shared/
├── types/              # Общие TypeScript/Python типы
│   ├── user.ts
│   ├── article.ts
│   └── response.ts
├── constants/          # Константы
│   ├── status.ts
│   └── errors.ts
├── utils/              # Общие утилиты
│   └── validators.ts
└── schemas/            # JSON schemas для валидации
    └── article.json
```

## 📦 Использование

### В Backend (Python)
```python
# Пока не используется, планируется для будущих версий
```

### В Frontend (TypeScript)
```typescript
import { UserType, ArticleStatus } from '../shared/types'
import { validateArticleNumber } from '../shared/utils/validators'
```

### В Bot (Python)
```python
# Пока не используется, планируется для будущих версий
```

## 🎯 Назначение

Этот модуль содержит код, который может использоваться в нескольких компонентах проекта:
- Типы данных
- Валидаторы
- Константы
- Общие утилиты

## 📝 Примечание

На данном этапе MVP большинство логики специфична для каждого компонента.
Shared модуль будет активно использоваться в будущих версиях для:
- Синхронизации типов между backend и frontend
- Общих бизнес-правил
- Единых валидаторов

