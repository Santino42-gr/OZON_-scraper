# Отключение автоматических задач мониторинга

Документация по отключению автоматических задач, которые обращаются к Parser Market API по расписанию.

---

## 📋 Проблема

Автоматические задачи по расписанию обращаются к Parser Market API и собирают данные, что приводит к лишним расходам. Необходимо полностью отключить все автоматические задачи мониторинга.

---

## ✅ Что было отключено

### 1. Scheduler в Backend (backend/services/scheduler.py)

**Задачи, которые были отключены:**
- **Обновление снэпшотов сравнений** - запускалось каждый день в 03:00
- **Обновление истории цен** - запускалось каждый день в 04:00

**Где отключено:**
- `backend/main.py` - добавлена проверка переменной `ENABLE_SCHEDULER`
- По умолчанию `ENABLE_SCHEDULER=false`, scheduler не запускается

### 2. Cron задачи в Docker (docker-compose.cron.yml)

**Задачи, которые были отключены:**
- **price_updater** - запускался в 09:00 каждый день
- **price_updater** - запускался в 15:00 каждый день

**Где отключено:**
- `docker-compose.cron.yml` - сервис `cron-worker` закомментирован
- Контейнер не будет запускаться при использовании `docker-compose.cron.yml`

---

## 🔧 Текущее состояние

### Автоматические задачи: **ОТКЛЮЧЕНЫ** ❌

- ❌ Scheduler не запускается при старте backend
- ❌ Cron контейнер не запускается
- ✅ Данные собираются только по запросу пользователя (через бота или API)

### Что работает:

- ✅ Telegram бот работает нормально
- ✅ API endpoints работают нормально
- ✅ Ручной запрос данных через бота работает
- ✅ Ручной запрос данных через API работает

---

## 🚀 Как включить обратно (если нужно)

### Включить Scheduler в Backend

1. Добавьте в `.env` файл:
```bash
ENABLE_SCHEDULER=true
```

2. Перезапустите backend:
```bash
# Если используете Docker
docker-compose restart backend

# Если запускаете напрямую
# Просто перезапустите приложение
```

### Включить Cron задачи

1. Откройте `docker-compose.cron.yml`

2. Раскомментируйте сервис `cron-worker` (уберите символы `#` в начале строк)

3. Перезапустите контейнеры:
```bash
docker-compose -f docker-compose.yml -f docker-compose.cron.yml up -d cron-worker
```

---

## 📊 Расписание задач (если включены)

### Scheduler (backend/services/scheduler.py)

| Задача | Расписание | Время | Описание |
|--------|-----------|-------|----------|
| update_comparison_snapshots | Ежедневно | 03:00 | Обновление снэпшотов сравнений |
| update_price_history | Ежедневно | 04:00 | Обновление истории цен |

### Cron задачи (docker-compose.cron.yml)

| Задача | Расписание | Время | Описание |
|--------|-----------|-------|----------|
| price_updater | Ежедневно | 09:00 | Обновление цен (once + twice) |
| price_updater | Ежедневно | 15:00 | Обновление цен (только twice) |

---

## 💰 Экономия расходов

После отключения автоматических задач:

- ✅ **Нет автоматических запросов к Parser Market API**
- ✅ **Данные собираются только по запросу пользователя**
- ✅ **Значительное снижение расходов на API**

### Пример экономии:

Если у вас 80 артикулов:
- **До отключения:** 80 запросов × 4 раза в день = **320 запросов/день**
- **После отключения:** только по запросу пользователя = **~0-10 запросов/день**

**Экономия: ~97% расходов на API**

---

## 🔍 Проверка состояния

### Проверить, запущен ли scheduler

В логах backend при старте должно быть:
```
⚠️  Scheduler disabled (ENABLE_SCHEDULER=false). Automatic monitoring tasks are OFF.
```

Если видите:
```
✅ Scheduler enabled and started
```
Значит scheduler включен.

### Проверить, запущен ли cron контейнер

```bash
docker ps | grep cron
```

Если контейнер не запущен - всё правильно, cron отключен.

---

## 📝 Файлы, которые были изменены

1. **backend/main.py**
   - Добавлена проверка `ENABLE_SCHEDULER` перед запуском scheduler
   - Добавлены комментарии об отключении

2. **backend/config.py**
   - Добавлена переменная `ENABLE_SCHEDULER: bool = False`

3. **docker-compose.cron.yml**
   - Сервис `cron-worker` закомментирован
   - Добавлены комментарии с инструкциями

---

## ⚠️ Важно

- После отключения автоматических задач **данные не будут обновляться автоматически**
- Пользователи должны **вручную запрашивать обновление** через бота или API
- История цен **не будет собираться автоматически**
- Снэпшоты сравнений **не будут обновляться автоматически**

Если нужны автоматические обновления, но реже - можно:
1. Изменить расписание в `scheduler.py` (например, раз в неделю)
2. Уменьшить количество артикулов для автоматического обновления
3. Использовать фильтры по `report_frequency`

---

## 🆘 Поддержка

Если возникли вопросы:
1. Проверьте логи backend: `backend/logs/backend.log`
2. Проверьте переменные окружения в `.env`
3. Убедитесь, что cron контейнер не запущен

---

**Дата отключения:** 2025-11-05  
**Причина:** Экономия расходов на Parser Market API  
**Статус:** ✅ Все автоматические задачи отключены

