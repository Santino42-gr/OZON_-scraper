# OZON API - Документация и Интеграция

Полная документация по интеграции с OZON Seller API для MVP проекта.

## 📋 Обзор

OZON предоставляет два основных API для продавцов:

### 1. **Seller API** (Основной для нашего MVP)
Для управления товарами, ценами, остатками и заказами.

### 2. **Performance API**
Для аналитики и мониторинга производительности (будет использоваться в будущем).

## 🔑 Аутентификация

### Требования
Для работы с OZON API необходимо:
- ✅ Зарегистрированный seller-аккаунт на OZON
- ✅ Client-Id (идентификатор клиента)
- ✅ API-Key (API ключ с правами "Admin")

### Получение ключей

1. Войдите в личный кабинет продавца: https://seller.ozon.ru
2. Перейдите в **Настройки → API ключи**
3. Скопируйте ваш **Client-Id**
4. Нажмите **"Создать ключ"**:
   - Укажите название ключа (например, "OZON Bot MVP")
   - Выберите тип токена: **"Admin"** (полные права)
5. Сохраните сгенерированный **API-Key** (показывается только один раз!)

### Формат запросов

Все запросы к OZON API требуют заголовки:

```http
Client-Id: ваш-client-id
Api-Key: ваш-api-key
Content-Type: application/json
```

## 📚 Документация

- **Официальная документация**: https://docs.ozon.com/global/api/
- **Postman коллекция**: https://www.postman.com/googlesheets/ozon-seller-api/
- **Базы знаний**: https://seller.ozon.ru/docs/work-with-ozon/api

## 🎯 Необходимые Endpoints для MVP

### 1. Получение информации о товаре

#### `/v2/product/info`
Получить полную информацию о товаре по SKU или артикулу.

**Request:**
```json
POST /v2/product/info
{
  "offer_id": "",
  "product_id": 123456789,
  "sku": 987654321
}
```

**Response:**
```json
{
  "result": {
    "id": 123456789,
    "name": "Название товара",
    "offer_id": "артикул-продавца",
    "barcode": "4607123456789",
    "price": "1999.00",
    "old_price": "2499.00",
    "vat": "0.20",
    "currency_code": "RUB",
    "quantity": 10,
    "visible": true,
    "status": {
      "state": "processed",
      "state_failed": "",
      "moderate_status": "approved"
    }
  }
}
```

### 2. Получение цены товара

#### `/v1/product/info/prices`
Получить актуальные цены товаров.

**Request:**
```json
POST /v1/product/info/prices
{
  "filter": {
    "product_id": [123456789],
    "visibility": "ALL"
  }
}
```

**Response:**
```json
{
  "result": {
    "items": [
      {
        "product_id": 123456789,
        "offer_id": "артикул",
        "price": {
          "price": "1999.00",
          "old_price": "2499.00",
          "currency_code": "RUB"
        },
        "marketing_price": "1899.00"
      }
    ]
  }
}
```

### 3. Получение остатков

#### `/v3/product/info/stocks`
Получить информацию об остатках товаров на складе.

**Request:**
```json
POST /v3/product/info/stocks
{
  "filter": {
    "product_id": [123456789],
    "visibility": "ALL"
  }
}
```

**Response:**
```json
{
  "result": {
    "items": [
      {
        "product_id": 123456789,
        "offer_id": "артикул",
        "stocks": [
          {
            "type": "fbo",
            "present": 10,
            "reserved": 2
          },
          {
            "type": "fbs",
            "present": 5,
            "reserved": 1
          }
        ]
      }
    ]
  }
}
```

### 4. Поиск товаров по артикулу

#### `/v2/product/list`
Поиск товаров в каталоге продавца.

**Request:**
```json
POST /v2/product/list
{
  "filter": {
    "offer_id": ["артикул-1", "артикул-2"],
    "visibility": "ALL"
  },
  "last_id": "",
  "limit": 100
}
```

## 🔒 Rate Limits

OZON API имеет ограничения на количество запросов:

- **Основные методы**: ~30 запросов в минуту
- **Bulk операции**: ~5 запросов в минуту
- **Рекомендация**: Использовать экспоненциальный backoff при ошибках 429

### Обработка Rate Limits

```python
import time
from functools import wraps

def retry_with_backoff(max_retries=3, base_delay=1):
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            for attempt in range(max_retries):
                try:
                    return func(*args, **kwargs)
                except RateLimitError:
                    if attempt == max_retries - 1:
                        raise
                    delay = base_delay * (2 ** attempt)
                    time.sleep(delay)
        return wrapper
    return decorator
```

## 🐍 Python Библиотеки

### Официальная библиотека
Нет официальной библиотеки от OZON.

### Сторонние библиотеки
- **ozon-api** (PyPI): https://pypi.org/project/ozon-api/
  - Базовая обертка над API
  - Не поддерживается активно
  - Рекомендуется для изучения структуры

### Рекомендация для MVP
Создать собственный клиент на основе `httpx` или `aiohttp` для:
- Полного контроля над запросами
- Async поддержки
- Кастомной обработки ошибок
- Легкого расширения функционала

## ⚠️ Ограничения OZON Seller API

### Критические ограничения:

1. **Требуется seller-аккаунт**
   - ❌ Нельзя получить данные о чужих товарах через API
   - ✅ Можно только управлять своими товарами
   - **Вывод**: Seller API не подходит для мониторинга чужих товаров!

2. **Отсутствие Public API**
   - OZON не предоставляет публичный API для поиска товаров
   - Нет endpoints для получения данных из маркетплейса как покупатель

3. **Доступ только к своему каталогу**
   - API работает только с товарами, добавленными вашим seller-аккаунтом

## 🔄 Fallback Стратегия: Web Scraping

### Причина
Поскольку OZON Seller API не позволяет мониторить товары других продавцов, **необходимо использовать web scraping**.

### Архитектура решения

```
┌─────────────────┐
│   User Request  │
│   (артикул)     │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│   OZON Service (Python)         │
│                                 │
│  1. Формирование URL товара     │
│  2. Отправка запроса            │
│  3. Парсинг HTML/JSON           │
│  4. Извлечение данных           │
│  5. Кеширование результата      │
└─────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   Response                      │
│   {                             │
│     "name": "...",              │
│     "price": 1999,              │
│     "rating": 4.5,              │
│     "reviews": 123,             │
│     "available": true           │
│   }                             │
└─────────────────────────────────┘
```

### Технологии для Scraping

#### Вариант 1: httpx + BeautifulSoup (Легковесный)
- ✅ Быстро
- ✅ Низкое потребление ресурсов
- ❌ Может не работать с динамическим контентом
- ❌ Легко блокируется

#### Вариант 2: Playwright (Рекомендуется для MVP)
- ✅ Поддержка JavaScript
- ✅ Обход антибот систем
- ✅ Скриншоты для отладки
- ❌ Медленнее
- ❌ Требует больше ресурсов

#### Вариант 3: Selenium (Альтернатива)
- ✅ Зрелая библиотека
- ✅ Хорошая документация
- ❌ Медленнее Playwright
- ❌ Сложнее в настройке

### Рекомендуемый стек для MVP:
1. **Основной метод**: Playwright (async)
2. **Fallback**: httpx + BeautifulSoup
3. **Кеширование**: Redis (TTL: 1 час)
4. **User-Agent**: ротация через библиотеку `fake-useragent`
5. **Proxy**: опционально, для масштабирования

## 🛠️ Реализация для MVP

### Структура OZON Service

```python
# backend/services/ozon_service.py

class OzonService:
    """
    Сервис для получения информации о товарах OZON
    Использует web scraping (т.к. Public API отсутствует)
    """
    
    async def get_product_info(self, article: str) -> ProductInfo:
        """Получить информацию о товаре по артикулу"""
        pass
    
    async def get_product_price(self, article: str) -> float:
        """Получить текущую цену товара"""
        pass
    
    async def check_availability(self, article: str) -> bool:
        """Проверить наличие товара"""
        pass
    
    async def get_product_rating(self, article: str) -> dict:
        """Получить рейтинг и отзывы"""
        pass
```

## 📈 Стратегия обработки ошибок

### Типы ошибок

1. **Network Errors** (Timeout, Connection)
   - Retry с экспоненциальным backoff
   - Максимум 3 попытки

2. **Anti-Bot (403, Captcha)**
   - Смена User-Agent
   - Добавление задержки
   - Использование Playwright

3. **Product Not Found (404)**
   - Логирование
   - Возврат `None`

4. **Rate Limit (429)**
   - Задержка перед повтором
   - Использование кеша

### Пример обработки

```python
async def safe_fetch(url: str, max_retries: int = 3):
    for attempt in range(max_retries):
        try:
            response = await client.get(url)
            return response
        except httpx.TimeoutException:
            if attempt == max_retries - 1:
                raise
            await asyncio.sleep(2 ** attempt)
        except httpx.HTTPStatusError as e:
            if e.response.status_code == 429:
                await asyncio.sleep(5)
            elif e.response.status_code == 403:
                # Switch to Playwright
                return await fetch_with_playwright(url)
            else:
                raise
```

## 🎯 План интеграции для MVP

### Phase 1: Базовый Scraping ✅
1. Создать `OzonService` с Playwright
2. Реализовать получение:
   - Название товара
   - Цена
   - Наличие
   - Изображение
3. Добавить кеширование (Redis/Memory)

### Phase 2: Расширенные данные
1. Рейтинг и отзывы
2. История изменения цен
3. Характеристики товара

### Phase 3: Оптимизация
1. Batch запросы
2. Proxy ротация
3. Monitoring и alerting

## 🔐 Безопасность

### Best Practices

1. **User-Agent ротация**
   ```python
   from fake_useragent import UserAgent
   ua = UserAgent()
   headers = {'User-Agent': ua.random}
   ```

2. **Задержки между запросами**
   ```python
   import random
   await asyncio.sleep(random.uniform(1, 3))
   ```

3. **Respect robots.txt**
   - Проверить разрешенные пути
   - Следовать ограничениям crawl-delay

4. **Логирование всех запросов**
   - Для отладки и мониторинга
   - Сохранение в таблицу `logs`

## 📊 Мониторинг

### Метрики для отслеживания

- ✅ Успешность запросов (Success Rate)
- ✅ Среднее время ответа
- ✅ Количество 403/429 ошибок
- ✅ Cache Hit Rate
- ✅ Количество запросов в минуту

### Alerts

- 🔴 Success Rate < 80%
- 🟡 Avg Response Time > 5s
- 🔴 Слишком много 403 ошибок

## 📝 Заключение

### Финальное решение для MVP:

**❌ OZON Seller API** - не подходит (требует seller-аккаунт, только свои товары)

**✅ Web Scraping** - основной метод:
- Playwright для динамического контента
- httpx + BeautifulSoup как fallback
- Redis кеширование (TTL: 1 час)
- Обработка ошибок и retry логика
- User-Agent ротация

### Следующие шаги:
1. ✅ Документация создана
2. ⏭️ Реализация `OzonService` с Playwright
3. ⏭️ Тесты и отладка
4. ⏭️ Интеграция с Backend API

---

**Дата создания:** 2025-10-18  
**Статус:** ✅ Анализ завершен  
**Решение:** Web Scraping (Playwright)


