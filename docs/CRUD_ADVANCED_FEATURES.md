# Расширенные функции CRUD страниц (AIL-293)

## 🎯 Обзор

Реализованы расширенные функции для CRUD страниц админ-панели OZON Bot:
- Модальные окна для просмотра и редактирования
- Массовые операции с checkbox
- Экспорт данных в CSV
- Детальный просмотр логов
- Debounced поиск
- История артикулов пользователей

## 🆕 Новые функции

### 1. **Страница Артикулы** (Articles)

#### Модальные окна

**Просмотр деталей:**
- Полная информация об артикуле
- JSON данные последней проверки OZON
- Информация о пользователе
- Статусы и метаданные

```tsx
<Dialog open={!!viewArticle} onOpenChange={() => setViewArticle(null)}>
  <DialogContent className="max-w-2xl">
    <DialogHeader>
      <DialogTitle>Детали артикула {viewArticle?.articleNumber}</DialogTitle>
    </DialogHeader>
    {/* Детальная информация */}
  </DialogContent>
</Dialog>
```

**Редактирование:**
- Изменение статуса артикула
- Реактивная форма с валидацией
- Мгновенное обновление UI

```tsx
<Dialog open={!!editArticle} onOpenChange={() => setEditArticle(null)}>
  <DialogContent>
    <Select value={editStatus} onValueChange={(v) => setEditStatus(v as Article['status'])}>
      {/* Выбор статуса */}
    </Select>
  </DialogContent>
</Dialog>
```

#### Массовый выбор

- Checkbox для каждой строки
- "Выбрать все" в заголовке таблицы
- Кнопка массового удаления
- Счетчик выбранных элементов

**Функции:**
```typescript
const toggleSelectAll = () => {
  if (selectedIds.length === data?.data.length) {
    setSelectedIds([]);
  } else {
    setSelectedIds(data?.data.map(a => a.id) || []);
  }
};

const deleteMultipleMutation = useMutation({
  mutationFn: mockCrudService.deleteMultipleArticles,
  onSuccess: () => {
    setSelectedIds([]);
    toast.success(`Удалено артикулов: ${selectedIds.length}`);
  },
});
```

#### Экспорт в CSV

- Экспорт текущих фильтрованных данных
- Автоматическая генерация CSV файла
- Скачивание с датой в имени файла

```typescript
const exportMutation = useMutation({
  mutationFn: () => mockCrudService.exportArticles({
    search: debouncedSearch,
    status: statusFilter,
  }),
  onSuccess: (csv) => {
    const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
    const link = document.createElement('a');
    link.href = URL.createObjectURL(blob);
    link.download = `articles_${new Date().toISOString().split('T')[0]}.csv`;
    link.click();
    toast.success('Экспорт завершен');
  },
});
```

#### Debounced поиск

- Задержка 300мс перед запросом
- Уменьшение нагрузки на API
- Плавный UX без лагов

```typescript
const handleSearchChange = useCallback((value: string) => {
  setSearch(value);
  const timer = setTimeout(() => setDebouncedSearch(value), 300);
  return () => clearTimeout(timer);
}, []);
```

### 2. **Страница Пользователи** (Users)

#### Просмотр артикулов пользователя

- Модальное окно с таблицей артикулов
- Фильтрация по пользователю
- Детальная информация по каждому артикулу

```tsx
<Dialog open={!!selectedUser} onOpenChange={() => setSelectedUser(null)}>
  <DialogContent className="max-w-4xl">
    <Table>
      {/* Таблица артикулов пользователя */}
    </Table>
  </DialogContent>
</Dialog>
```

**Функции:**
```typescript
const { data: articlesData, isLoading: articlesLoading } = useQuery<Article[]>({
  queryKey: ['user-articles', selectedUser?.id],
  queryFn: () => mockCrudService.getUserArticles(selectedUser!.id),
  enabled: !!selectedUser,
});
```

#### Расширенные действия

**Dropdown меню:**
- Просмотр артикулов пользователя
- Блокировка/разблокировка
- Цветовая индикация статуса

```tsx
<DropdownMenu>
  <DropdownMenuItem onClick={() => handleViewArticles(user)}>
    <Package className="mr-2 h-4 w-4" />
    Артикулы пользователя
  </DropdownMenuItem>
  <DropdownMenuSeparator />
  <DropdownMenuItem
    className={user.isBlocked ? 'text-green-600' : 'text-destructive'}
  >
    {user.isBlocked ? 'Разблокировать' : 'Заблокировать'}
  </DropdownMenuItem>
</DropdownMenu>
```

### 3. **Страница Логи** (Logs)

#### Детальный просмотр

- Модальное окно с полной информацией о событии
- Метаданные в JSON формате
- Информация о пользователе
- Связанный артикул (если есть)

```tsx
<Dialog open={!!selectedLog} onOpenChange={() => setSelectedLog(null)}>
  <DialogContent className="max-w-3xl max-h-[80vh] overflow-y-auto">
    <DialogHeader>
      <Badge variant={getLevelBadge(selectedLog.level)}>
        {getLevelLabel(selectedLog.level)}
      </Badge>
      {selectedLog?.eventType}
    </DialogHeader>
    {/* Детальная информация */}
  </DialogContent>
</Dialog>
```

**Отображение метаданных:**
```tsx
{selectedLog.metadata && (
  <div>
    <Label>Метаданные</Label>
    <pre className="mt-1 bg-muted p-4 rounded-md text-xs overflow-x-auto">
      {JSON.stringify(selectedLog.metadata, null, 2)}
    </pre>
  </div>
)}
```

## 🔧 Технические улучшения

### Mock API расширения

Добавлены новые методы в `mockCrudService.ts`:

```typescript
// Articles
async getArticleById(id: string): Promise<Article | null>
async updateArticle(id: string, data: Partial<Article>): Promise<Article>
async deleteMultipleArticles(ids: string[]): Promise<void>
async exportArticles(filters: {...}): Promise<string>

// Users  
async getUserById(id: string): Promise<User | null>
async getUserArticles(userId: string): Promise<Article[]>

// Logs
async getLogById(id: string): Promise<Log | null>
```

### Debounced поиск на всех страницах

**Преимущества:**
- Уменьшение количества запросов к API
- Плавный UX без задержек ввода
- Экономия ресурсов сервера

**Реализация:**
```typescript
const [search, setSearch] = useState('');
const [debouncedSearch, setDebouncedSearch] = useState('');

const handleSearchChange = useCallback((value: string) => {
  setSearch(value); // Мгновенное обновление input
  const timer = setTimeout(() => setDebouncedSearch(value), 300);
  return () => clearTimeout(timer); // Очистка таймера
}, []);

// Использование в запросе
const { data } = useQuery({
  queryKey: ['data', { search: debouncedSearch }],
  queryFn: () => api.getData({ search: debouncedSearch }),
});
```

### React Query оптимизации

**Оптимистичные обновления:**
```typescript
const deleteMutation = useMutation({
  mutationFn: mockCrudService.deleteArticle,
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['articles'] });
    toast.success('Артикул удален');
  },
});
```

**Условные запросы:**
```typescript
const { data: articlesData } = useQuery<Article[]>({
  queryKey: ['user-articles', selectedUser?.id],
  queryFn: () => mockCrudService.getUserArticles(selectedUser!.id),
  enabled: !!selectedUser, // Запрос только когда есть selectedUser
});
```

## 🎨 UI/UX улучшения

### Toast уведомления (Sonner)

Для всех действий добавлены toast уведомления:
- Успешное удаление
- Ошибки операций
- Блокировка/разблокировка пользователей
- Экспорт данных

```typescript
import { toast } from 'sonner';

toast.success('Успешно');
toast.error('Ошибка');
```

### Модальные окна (Dialog)

**Особенности:**
- Адаптивные размеры (max-w-2xl, max-w-4xl)
- Прокрутка для больших данных
- Анимации открытия/закрытия
- Управление фокусом

### Улучшенные таблицы

**Features:**
- Checkbox для массового выбора
- Dropdown меню для действий
- Адаптивные колонки
- Loading states со Skeleton

## 📊 Статистика

### Добавленные функции:
- ✅ 3 модальных окна для просмотра деталей
- ✅ 2 модальных окна для редактирования  
- ✅ Массовый выбор и удаление
- ✅ Экспорт в CSV
- ✅ Debounced поиск на всех страницах
- ✅ Просмотр артикулов пользователя
- ✅ Детальный просмотр логов

### Обновленные файлы:
```
frontend/src/
├── services/
│   └── mockCrudService.ts         # +7 методов
├── pages/
│   ├── Articles.tsx               # Полностью переработан
│   ├── Users.tsx                  # Добавлен просмотр артикулов
│   └── Logs.tsx                   # Добавлен детальный просмотр
└── components/ui/
    ├── button.tsx                 # Исправлены импорты
    ├── calendar.tsx               # Новый компонент
    └── command.tsx                # Новый компонент
```

### Метрики кода:
- **Новых строк кода:** ~500
- **Новых компонентов:** 5 модальных окон
- **Новых API методов:** 7
- **Ошибок TypeScript:** 0 ✅

## 🚀 Использование

### Массовое удаление артикулов
```
1. Выберите артикулы через checkbox
2. Нажмите "Удалить (N)"
3. Подтвердите действие
```

### Экспорт данных
```
1. Примените нужные фильтры
2. Нажмите кнопку "Экспорт"
3. Файл автоматически скачается
```

### Просмотр артикулов пользователя
```
1. Нажмите на меню действий у пользователя
2. Выберите "Артикулы пользователя"
3. Откроется модальное окно с таблицей
```

### Детальный просмотр лога
```
1. Нажмите на иконку "глаз" в строке лога
2. Откроется модальное окно с полной информацией
3. Просмотрите метаданные и связанные сущности
```

## 🔮 Следующие шаги

### Планируемые улучшения:
1. **Интеграция с реальным API:**
   - Заменить mock на реальные endpoint'ы
   - Добавить обработку ошибок API
   - Настроить авторизацию

2. **Расширение функционала:**
   - Массовое редактирование
   - Экспорт в Excel/XLSX
   - Фильтры по датам с Calendar
   - История изменений

3. **Оптимизация:**
   - Виртуализация таблиц (@tanstack/react-virtual)
   - Бесконечная прокрутка
   - Кеширование экспортов
   - Оптимизация ререндеров

4. **UX улучшения:**
   - Горячие клавиши (Ctrl+A для выбора всех)
   - Drag & drop для сортировки
   - Inline редактирование
   - Расширенные фильтры

## 📝 Примечания

### Debounced поиск
- Задержка 300мс оптимальна для большинства случаев
- Для более быстрых API можно уменьшить до 200мс
- Для медленных API увеличить до 500мс

### CSV экспорт
- Текущая реализация экспортирует только текущие фильтры
- Для полного экспорта нужно убрать фильтры
- Максимальный размер файла ограничен памятью браузера

### Модальные окна
- Используют shadcn/ui Dialog компонент
- Адаптивные и доступные (a11y)
- Управляются через состояние React

---

**Статус:** ✅ Завершено  
**Дата:** 2025-01-20  
**Задача:** AIL-293  
**Предыдущая:** AIL-292


