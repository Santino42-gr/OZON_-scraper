# Comparison Frontend - Руководство

> **Версия:** 1.0.0
> **Технологии:** React + TypeScript + shadcn/ui + Recharts

---

## 📋 Обзор

Frontend компоненты для функционала сравнения артикулов OZON.

### Созданные файлы:

#### Services:
- ✅ `frontend/src/services/comparisonService.ts` - API клиент

#### Components:
- ✅ `frontend/src/components/comparison/ComparisonCard.tsx` - Карточка товара
- ✅ `frontend/src/components/comparison/ComparisonMetrics.tsx` - Метрики сравнения
- ✅ `frontend/src/components/comparison/QuickCompareDialog.tsx` - Диалог быстрого сравнения
- ✅ `frontend/src/components/comparison/ComparisonHistoryChart.tsx` - График истории
- ✅ `frontend/src/components/ui/progress.tsx` - Progress bar (общий компонент)

#### Pages:
- ✅ `frontend/src/pages/Comparison.tsx` - Главная страница

#### Routing:
- ✅ `frontend/src/App.tsx` - Добавлен роут `/comparison`
- ✅ `frontend/src/components/common/Layout.tsx` - Добавлена ссылка в навигацию

---

## 🚀 Быстрый старт

### 1. Установка зависимостей (если нужно)

```bash
cd frontend
npm install
```

Компоненты используют уже установленные библиотеки:
- `@radix-ui/*` - UI primitives (уже есть)
- `recharts` - Графики (нужно проверить)
- `lucide-react` - Иконки (уже есть)
- `axios` - HTTP клиент (уже есть)

### 2. Проверка recharts

```bash
npm install recharts
```

Или добавить в `package.json`:
```json
{
  "dependencies": {
    "recharts": "^2.10.0"
  }
}
```

### 3. Запуск

```bash
npm run dev
```

Откройте http://localhost:5173/comparison

---

## 📊 Структура компонентов

### ComparisonCard

Карточка отображения артикула для сравнения.

**Props:**
```typescript
interface ComparisonCardProps {
  article: ArticleComparisonData;
  isOwn?: boolean; // Выделяет "свой" товар
}
```

**Что показывает:**
- Роль (Ваш товар / Конкурент)
- Название и артикул
- Изображение
- Цены (с картой, без карты, старая, средняя)
- Рейтинг и количество отзывов
- СПП метрики (СПП1, СПП2, Общий)
- Ссылка на OZON

**Использование:**
```tsx
<ComparisonCard
  article={ownProduct}
  isOwn={true}
/>
```

---

### ComparisonMetrics

Отображает метрики сравнения с визуализацией.

**Props:**
```typescript
interface ComparisonMetricsProps {
  metrics: ComparisonMetrics;
}
```

**Что показывает:**
- Индекс конкурентоспособности (0-100%)
- Грейд (A-F) с цветовой индикацией
- Progress bar
- Детальное сравнение по 4 метрикам:
  - 💰 Цена
  - ⭐ Рейтинг
  - 🏷️ СПП
  - 💬 Отзывы
- Рекомендации по каждой метрике
- Общую рекомендацию
- Шкалу грейдов

**Использование:**
```tsx
{comparison.metrics && (
  <ComparisonMetrics metrics={comparison.metrics} />
)}
```

---

### QuickCompareDialog

Модальное окно для быстрого создания сравнения 1v1.

**Props:**
```typescript
interface QuickCompareDialogProps {
  userId: string;
  onSuccess?: (comparison: ComparisonResponse) => void;
  trigger?: React.ReactNode; // Кастомная кнопка
}
```

**Что делает:**
- Принимает 2 артикула
- Опционально название группы
- Создает группу
- Добавляет артикулы
- Получает данные с OZON
- Рассчитывает метрики
- Возвращает результат через callback

**Использование:**
```tsx
<QuickCompareDialog
  userId={userId}
  onSuccess={(comparison) => {
    setActiveComparison(comparison);
  }}
/>
```

---

### ComparisonHistoryChart

График изменения индекса конкурентоспособности во времени.

**Props:**
```typescript
interface ComparisonHistoryChartProps {
  history: ComparisonHistoryResponse;
  loading?: boolean;
}
```

**Что показывает:**
- Line chart (Recharts)
- Индекс конкурентоспособности по датам
- Тренд (изменение с последнего снэпшота)
- Статистику: минимум, средний, максимум
- Количество снэпшотов

**Использование:**
```tsx
<ComparisonHistoryChart
  history={history}
  loading={loading}
/>
```

---

### Comparison Page

Главная страница сравнения.

**URL:** `/comparison`

**Состояния:**
1. **Empty state** - Нет активного сравнения
   - Показывает приглашение начать
   - Кнопка "Быстрое сравнение"

2. **Comparison view** - Активное сравнение
   - 2 вкладки: "Сравнение" и "История"
   - Карточки товаров
   - Метрики (если 1v1)
   - Кнопка обновления
   - Индикатор свежести данных

3. **Loading state** - Загрузка данных
   - Skeleton screens

**Функционал:**
- Статистика пользователя (4 карточки)
- Создание сравнения
- Просмотр результатов
- Обновление данных
- Просмотр истории

---

## 🎨 API Service

### comparisonService.ts

**Основные методы:**

```typescript
// Быстрое сравнение (рекомендуется)
comparisonApi.quickCompare(userId, {
  own_article_number: '123',
  competitor_article_number: '456',
  scrape_now: true
})

// Создать группу
comparisonApi.createGroup(userId, {
  name: 'My Comparison',
  group_type: 'comparison'
})

// Получить сравнение
comparisonApi.getComparison(groupId, userId, refresh)

// История
comparisonApi.getHistory(groupId, userId, days)

// Статистика
comparisonApi.getUserStats(userId)
```

**Helper functions:**

```typescript
// Цвет грейда
getGradeColor('A') // 'bg-green-500'

// Описание грейда
getGradeDescription('C') // 'Средняя конкурентоспособность'

// Форматирование
formatPrice(5000) // '5 000 ₽'
formatPercentage(15.5) // '+15.5%'
formatRating(4.7) // '4.7'
```

---

## 🔧 Кастомизация

### Изменить цвета грейдов

В `comparisonService.ts`:

```typescript
export function getGradeColor(grade: string): string {
  switch (grade) {
    case 'A': return 'bg-emerald-500'; // Вместо green-500
    // ...
  }
}
```

### Изменить веса метрик

Веса задаются на backend в `comparison_service.py`:

```python
WEIGHTS = {
    'price': 0.35,       # Можно изменить
    'rating': 0.25,
    'spp': 0.20,
    'reviews': 0.10,
    'availability': 0.10
}
```

### Добавить свою метрику

1. Обновить типы в `comparisonService.ts`
2. Обновить `ComparisonMetrics.tsx`
3. Добавить в backend расчет

---

## 🧪 Тестирование

### Ручное тестирование

1. Запустите backend:
```bash
cd backend
uvicorn main:app --reload
```

2. Запустите frontend:
```bash
cd frontend
npm run dev
```

3. Откройте http://localhost:5173/comparison

4. Нажмите "Быстрое сравнение"

5. Введите артикулы:
   - Ваш: 123456789
   - Конкурент: 987654321

6. Нажмите "Сравнить"

7. Проверьте:
   - ✅ Карточки товаров отображаются
   - ✅ Метрики рассчитаны
   - ✅ Индекс и грейд показаны
   - ✅ Рекомендации понятны
   - ✅ История пустая (первое сравнение)

8. Обновите данные (кнопка Refresh)

9. Проверьте:
   - ✅ Появился второй снэпшот
   - ✅ График отображается
   - ✅ Тренд показан

---

## 📱 Адаптивность

Все компоненты адаптивны:

- **Desktop (> 768px):**
  - 2 колонки для карточек
  - Полная ширина метрик
  - Боковая навигация

- **Mobile (< 768px):**
  - 1 колонка
  - Скрытая навигация
  - Оптимизированные карточки

---

## ⚠️ Известные ограничения

1. **User ID захардкоден:**
   - В `Comparison.tsx` используется `MOCK_USER_ID`
   - Нужно заменить на реальный из AuthContext

2. **Recharts:**
   - Если не установлен, добавить в dependencies

3. **Progress component:**
   - Создан новый, но лучше использовать из shadcn/ui

4. **Error handling:**
   - Базовый, можно улучшить с retry логикой

---

## 🚀 Следующие шаги

### Улучшения UX:

1. **Добавить скелетоны** для загрузки
2. **Toast уведомления** при успехе/ошибке
3. **Подтверждение** перед удалением группы
4. **Поиск групп** если их много
5. **Фильтры** по грейдам

### Новые фичи:

1. **Экспорт** в PDF/Excel
2. **Сравнение 1vN** (один vs несколько)
3. **Автообновление** раз в час
4. **Уведомления** при изменении индекса
5. **Рекомендации с AI** (интеграция GPT)

---

## 📚 Полезные ссылки

- [shadcn/ui Docs](https://ui.shadcn.com/)
- [Recharts Docs](https://recharts.org/)
- [Radix UI](https://www.radix-ui.com/)
- [Lucide Icons](https://lucide.dev/)

---

**Статус:** ✅ Ready for use
**Последнее обновление:** 2025-10-30
