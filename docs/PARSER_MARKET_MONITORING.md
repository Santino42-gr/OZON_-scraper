# Управление заданиями мониторинга Parser Market

Документация по управлению автоматическими заданиями мониторинга Parser Market API.

---

## 📋 Проблема

Ежедневно с 06:00 до 07:00 приходит порядка 80 писем с мониторингом через Parser Market API. Задания создаются через API, но не видны в личном кабинете клиента, что приводит к лишним расходам.

---

## 🔍 Поиск заданий мониторинга

### Через скрипт (рекомендуется)

Используйте готовый скрипт для поиска и отключения заданий:

```bash
# Просмотр заданий без удаления (dry-run)
python -m scripts.disable_parser_market_monitoring --dry-run

# Отключение заданий с расписанием 06:00-07:00
python -m scripts.disable_parser_market_monitoring

# Настройка времени фильтрации
python -m scripts.disable_parser_market_monitoring --start-hour 6 --end-hour 7
```

### Через Python код

```python
from services.parser_market_client import ParserMarketClient
from config import settings

client = ParserMarketClient(
    api_key=settings.PARSER_MARKET_API_KEY,
    region=settings.PARSER_MARKET_REGION
)

# Получить все задания
all_tasks = await client.get_all_tasks(limit=1000)

# Фильтровать по времени (06:00-07:00)
monitoring_tasks = client.filter_tasks_by_time(
    all_tasks,
    start_hour=6,
    end_hour=7
)

print(f"Найдено заданий мониторинга: {len(monitoring_tasks)}")
```

---

## 🗑️ Отключение заданий

### Автоматическое отключение через скрипт

```bash
# Отключить все задания с расписанием 06:00-07:00
python -m scripts.disable_parser_market_monitoring
```

Скрипт:
1. Получает все задания через API
2. Фильтрует задания с указанным расписанием
3. Пытается отключить каждое задание через API
4. Выводит отчет о результатах

### Ручное отключение через код

```python
from services.parser_market_client import ParserMarketClient

client = ParserMarketClient(api_key="your_api_key")

# Получить задание
tasks = await client.get_task_status(limit=100)

# Отключить конкретное задание
for task in tasks:
    task_id = client.extract_task_id(task)
    userlabel = client.extract_userlabel(task)
    
    success = await client.delete_task(
        order_id=task_id,
        userlabel=userlabel
    )
    
    if success:
        print(f"Задание {task_id} отключено")
    else:
        print(f"Не удалось отключить задание {task_id}")
```

---

## ⚠️ Ограничения API

**Важно:** Parser Market API может не поддерживать удаление заданий через API. В этом случае:

1. **Скрипт попытается отключить задания**, но может не сработать
2. **Необходимо обратиться в поддержку Parser Market** с запросом:
   - Отключить все задания мониторинга для вашего API ключа
   - Указать, что задания создаются автоматически через API и не видны в личном кабинете
   - Попросить предоставить способ управления заданиями через API

### Контакты поддержки Parser Market

- Сайт: https://parser.market/
- Документация API: https://parser.market/parser-cen-json-api/
- Email поддержки: указан на сайте parser.market

---

## 🛡️ Предотвращение создания новых заданий

### При создании задач через API

Используйте параметр `disable_monitoring=True` при отправке задач:

```python
from services.parser_market_client import ParserMarketClient

client = ParserMarketClient(api_key="your_api_key")

# Отправить задачу БЕЗ автоматического мониторинга
result = await client.submit_task(
    article="123456789",
    disable_monitoring=True  # Отключить автоматический мониторинг
)
```

**Примечание:** Параметр `disable_monitoring` пытается предотвратить создание автоматических заданий мониторинга, но Parser Market API может не поддерживать этот параметр. В этом случае задания мониторинга могут все равно создаваться.

### В существующем коде

Все методы `submit_task()` в проекте теперь по умолчанию используют `disable_monitoring=True`:

- `backend/services/parser_market_client.py` - метод `submit_task()`
- `backend/cron_jobs/price_updater.py` - использует Parser Market клиент
- `backend/cron_jobs/price_history_collector.py` - использует Parser Market клиент

---

## 📊 Мониторинг заданий

### Проверка активных заданий

```python
from services.parser_market_client import ParserMarketClient

client = ParserMarketClient(api_key="your_api_key")

# Получить все задания
all_tasks = await client.get_all_tasks(limit=1000)

# Статистика по времени выполнения
from collections import Counter

hours = []
for task in all_tasks:
    task_dict = client._parse_task_dict(task)
    received = task_dict.get("received")
    if received and " " in received and ":" in received:
        time_part = received.split(" ")[-1]
        hour = int(time_part.split(":")[0])
        hours.append(hour)

hour_counts = Counter(hours)
print("Распределение заданий по часам:")
for hour, count in sorted(hour_counts.items()):
    print(f"  {hour:02d}:00 - {count} заданий")
```

### Проверка заданий мониторинга

```python
# Фильтровать задания с разными временными диапазонами
morning_tasks = client.filter_tasks_by_time(all_tasks, start_hour=6, end_hour=7)
afternoon_tasks = client.filter_tasks_by_time(all_tasks, start_hour=14, end_hour=15)

print(f"Заданий утром (06:00-07:00): {len(morning_tasks)}")
print(f"Заданий днем (14:00-15:00): {len(afternoon_tasks)}")
```

---

## 🔧 API методы для управления заданиями

### `get_all_tasks(limit=1000)`

Получить все задания через API.

**Параметры:**
- `limit` - максимальное количество заданий (по умолчанию 1000)

**Возвращает:**
- Список всех заданий с их статусами и метаданными

### `filter_tasks_by_time(tasks, start_hour=6, end_hour=7)`

Фильтровать задания по времени выполнения.

**Параметры:**
- `tasks` - список заданий
- `start_hour` - начальный час (0-23)
- `end_hour` - конечный час (0-23)

**Возвращает:**
- Отфильтрованный список заданий

### `delete_task(order_id=None, userlabel=None)`

Удалить или отключить задание мониторинга.

**Параметры:**
- `order_id` - ID задания
- `userlabel` - метка задания

**Возвращает:**
- `True` если задание успешно удалено, `False` иначе

**Примечание:** Может не работать, если Parser Market API не поддерживает удаление через API.

### `extract_task_id(task)`

Извлечь ID задания из данных задания.

**Параметры:**
- `task` - данные задания

**Возвращает:**
- ID задания или `None`

### `extract_userlabel(task)`

Извлечь userlabel из данных задания.

**Параметры:**
- `task` - данные задания

**Возвращает:**
- userlabel или `None`

---

## 📝 Примеры использования

### Пример 1: Найти и отключить все задания мониторинга

```python
import asyncio
from services.parser_market_client import ParserMarketClient
from config import settings

async def disable_all_monitoring():
    client = ParserMarketClient(
        api_key=settings.PARSER_MARKET_API_KEY,
        region=settings.PARSER_MARKET_REGION
    )
    
    try:
        # Получить все задания
        all_tasks = await client.get_all_tasks(limit=1000)
        
        # Фильтровать задания мониторинга (06:00-07:00)
        monitoring_tasks = client.filter_tasks_by_time(
            all_tasks,
            start_hour=6,
            end_hour=7
        )
        
        # Отключить каждое задание
        for task in monitoring_tasks:
            task_id = client.extract_task_id(task)
            userlabel = client.extract_userlabel(task)
            
            success = await client.delete_task(
                order_id=task_id,
                userlabel=userlabel
            )
            
            if success:
                print(f"✅ Задание {task_id} отключено")
            else:
                print(f"⚠️  Не удалось отключить задание {task_id}")
    
    finally:
        await client.close()

asyncio.run(disable_all_monitoring())
```

### Пример 2: Проверить наличие заданий мониторинга

```python
from services.parser_market_client import ParserMarketClient
from config import settings

client = ParserMarketClient(
    api_key=settings.PARSER_MARKET_API_KEY,
    region=settings.PARSER_MARKET_REGION
)

# Получить все задания
all_tasks = await client.get_all_tasks(limit=1000)

# Проверить разные временные диапазоны
time_ranges = [
    (6, 7, "06:00-07:00"),
    (9, 10, "09:00-10:00"),
    (15, 16, "15:00-16:00"),
]

for start, end, label in time_ranges:
    tasks = client.filter_tasks_by_time(all_tasks, start_hour=start, end_hour=end)
    print(f"{label}: {len(tasks)} заданий")

await client.close()
```

---

## 🚨 Решение проблем

### Проблема: Скрипт не находит задания

**Решение:**
1. Проверьте, что API ключ правильный
2. Увеличьте `limit` в `get_all_tasks()`
3. Проверьте формат времени в заданиях - возможно, нужно настроить парсинг

### Проблема: Не удается отключить задания через API

**Решение:**
1. Это нормально - Parser Market API может не поддерживать удаление через API
2. Обратитесь в поддержку Parser Market для отключения заданий
3. Укажите ваш API ключ и попросите отключить все задания мониторинга

### Проблема: Задания продолжают создаваться

**Решение:**
1. Убедитесь, что используете `disable_monitoring=True` при создании задач
2. Проверьте, что все места в коде используют обновленный клиент
3. Обратитесь в поддержку Parser Market для отключения автоматического создания заданий

---

## 📞 Поддержка

Если у вас возникли проблемы с управлением заданиями мониторинга:

1. **Проверьте логи** скрипта `disable_parser_market_monitoring.py`
2. **Обратитесь в поддержку Parser Market** с запросом на отключение заданий
3. **Создайте issue** в репозитории проекта с описанием проблемы

---

**Последнее обновление:** 2025-11-05

