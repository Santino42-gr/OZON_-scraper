# Показатели СПП (Скидки)

## Обзор

Реализована система расчета и отображения показателей скидки (СПП) для товаров OZON.

**СПП** - это процентные показатели, которые помогают анализировать эффективность скидок и динамику цен.

## Формулы расчета

### СПП1 - Скидка от средней цены
```
СПП1 = (Средняя цена за 7 дней - Обычная цена) / Средняя цена за 7 дней × 100%
```

**Что показывает:** На сколько процентов текущая обычная цена отличается от средней цены за последние 7 дней.

**Пример:** 
- Средняя цена за 7 дней: 1200₽
- Текущая обычная цена: 900₽
- СПП1 = (1200 - 900) / 1200 × 100% = **25.0%**

---

### СПП2 - Скидка Ozon Card
```
СПП2 = (Обычная цена - Цена с Ozon Card) / Обычная цена × 100%
```

**Что показывает:** Насколько процентов цена с Ozon Card выгоднее обычной цены.

**Пример:**
- Обычная цена: 900₽
- Цена с Ozon Card: 810₽
- СПП2 = (900 - 810) / 900 × 100% = **10.0%**

---

### СПП Общий - Общая скидка
```
СПП Общий = (Средняя цена за 7 дней - Цена с Ozon Card) / Средняя цена за 7 дней × 100%
```

**Что показывает:** Общий процент скидки от средней цены до цены с картой (комбинация СПП1 и СПП2).

**Пример:**
- Средняя цена за 7 дней: 1200₽
- Цена с Ozon Card: 810₽
- СПП Общий = (1200 - 810) / 1200 × 100% = **32.5%**

---

## База данных

### Таблица `ozon_scraper_articles`

Добавлены поля:
- `spp1` DECIMAL(5,2) - СПП1 в процентах
- `spp2` DECIMAL(5,2) - СПП2 в процентах  
- `spp_total` DECIMAL(5,2) - СПП Общий в процентах

### Таблица `ozon_scraper_price_history`

Добавлены поля для хранения истории СПП:
- `spp1` DECIMAL(5,2)
- `spp2` DECIMAL(5,2)
- `spp_total` DECIMAL(5,2)

### SQL Функции

#### `calculate_spp_metrics(article_number)`
Рассчитывает показатели СПП для артикула.

**Пример:**
```sql
SELECT * FROM calculate_spp_metrics('123456789');
```

**Результат:**
```
 spp1  | spp2  | spp_total
-------+-------+-----------
  25.0 |  10.0 |      32.5
```

#### `update_article_spp_metrics(article_number)`
Обновляет СПП в таблице `ozon_scraper_articles`.

**Пример:**
```sql
SELECT update_article_spp_metrics('123456789');
-- Возвращает: TRUE
```

#### `update_all_spp_metrics()`
Массово обновляет СПП для всех активных артикулов.

**Пример:**
```sql
SELECT update_all_spp_metrics();
-- Возвращает: 42 (количество обновленных записей)
```

---

## Backend API

### Endpoints

#### GET `/api/v1/articles/{article_id}/spp`
Получить показатели СПП для артикула.

**Response:**
```json
{
  "spp1": 25.0,
  "spp2": 10.0,
  "spp_total": 32.5
}
```

#### POST `/api/v1/articles/{article_id}/refresh-prices`
Обновить цены (автоматически пересчитывает СПП).

#### POST `/api/v1/articles/update-all-spp`
Массово обновить СПП для всех артикулов.

**Response:**
```json
{
  "success": true,
  "updated_count": 42,
  "message": "Показатели СПП обновлены для 42 артикулов"
}
```

---

## Python модели

### SPPMetrics
```python
from models.ozon_models import SPPMetrics

metrics = SPPMetrics(
    spp1=25.0,
    spp2=10.0,
    spp_total=32.5
)

# Форматирование
print(metrics.format_spp(25.0))  # "25.0%"
print(metrics.format_spp(None))  # "Н/Д"

# Словарь с форматированными значениями
formatted = metrics.to_dict_formatted()
# {"spp1": "25.0%", "spp2": "10.0%", "spp_total": "32.5%"}
```

### Расчет СПП в коде
```python
from services.ozon_scraper import OzonScraper

spp_metrics = OzonScraper.calculate_spp_metrics(
    average_price_7days=1200.0,
    normal_price=900.0,
    ozon_card_price=810.0
)

# Результат:
# {"spp1": 25.0, "spp2": 10.0, "spp_total": 32.5}
```

---

## Telegram Bot

### Отображение в сообщениях

При запросе информации об артикуле бот показывает СПП:

```
📦 Артикул: 123456789
💰 Цена: 900 ₽
💳 Без Ozon Card: 900 ₽
🎴 С Ozon Card: 810 ₽

📊 Показатели скидки:
  • СПП1: 25.0%
  • СПП2: 10.0%
  • СПП Общий: 32.5%
```

### Отображение в отчетах

СПП включены в отчеты пользователям:

```python
from bot.utils.formatters import format_report

report_data = {
    "article_number": "123456789",
    "spp1": 25.0,
    "spp2": 10.0,
    "spp_total": 32.5,
    # ... other data
}

message = format_report(report_data)
```

---

## Frontend (Админ-панель)

### Таблица артикулов

Добавлены колонки:
- **СПП1** - Скидка от средней до обычной цены
- **СПП2** - Скидка Ozon Card
- **СПП Общ** - Общая скидка

### Модальное окно деталей

При просмотре деталей артикула показывается красивая карточка с СПП метриками:

```
📊 Показатели скидки (СПП)

┌─────────────┬─────────────┬─────────────┐
│    СПП1     │    СПП2     │  СПП Общий  │
│    25.0%    │    10.0%    │    32.5%    │
│ Средняя →   │ Ozon Card   │   Общая     │
│  Обычная    │   скидка    │  скидка     │
└─────────────┴─────────────┴─────────────┘
```

---

## Cron Job

### Автоматический расчет

Cron job `price_history_collector.py` автоматически:
1. Собирает цены товаров
2. Рассчитывает СПП метрики
3. Сохраняет их в `ozon_scraper_price_history`

**Лог:**
```
✅ Successfully scraped 123456789: price=810.0, spp_total=32.5
```

---

## Обработка NULL значений

Если какая-либо цена недоступна (NULL), соответствующий СПП показатель:
- В БД сохраняется как `NULL`
- В API возвращается как `null`
- В боте/frontend отображается как **"Н/Д"** (недоступно)

**Пример:**
```json
{
  "spp1": 25.0,
  "spp2": null,    // Нет цены с Ozon Card
  "spp_total": null
}
```

В Telegram:
```
📊 Показатели скидки:
  • СПП1: 25.0%
  • СПП2: Н/Д
  • СПП Общий: Н/Д
```

---

## Примеры использования

### 1. Получить СПП через API
```bash
curl -X GET "http://localhost:8000/api/v1/articles/{article_id}/spp"
```

### 2. Обновить СПП после изменения цен
```python
# В роутере автоматически
supabase.rpc("update_article_spp_metrics", {"p_article_number": article_number})
```

### 3. Массовое обновление всех СПП
```bash
curl -X POST "http://localhost:8000/api/v1/articles/update-all-spp"
```

### 4. Расчет в Python коде
```python
from services.ozon_scraper import OzonScraper

# Получили данные о товаре
product_info = await scraper.get_product_info(article)

# Рассчитываем СПП
spp = OzonScraper.calculate_spp_metrics(
    product_info.average_price_7days,
    product_info.normal_price,
    product_info.ozon_card_price
)

print(f"СПП Общий: {spp['spp_total']}%")
```

---

## Миграция

**Файл:** `docs/migrations/007_add_spp_metrics_to_articles.sql`

**Применение:**
```bash
# Через Supabase Dashboard или MCP
# Migration 007 уже применена
```

**Проверка:**
```sql
-- Проверить структуру
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'ozon_scraper_articles' 
  AND column_name IN ('spp1', 'spp2', 'spp_total');

-- Тест функции
SELECT * FROM calculate_spp_metrics('123456789');
```

---

## Заключение

Система СПП метрик полностью интегрирована во все компоненты проекта:
- ✅ База данных (таблицы и функции)
- ✅ Backend API (endpoints и модели)
- ✅ Telegram Bot (форматтеры)
- ✅ Frontend (таблицы и детали)
- ✅ Cron Job (автоматический сбор)

Это позволяет эффективно анализировать динамику скидок и принимать обоснованные решения о покупках.

